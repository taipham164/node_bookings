<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="stylesheet" href="/stylesheets/admin-dashboard.css">
</head>
<body>
  <div class="admin-container">
    <aside class="admin-sidebar">
      <div class="admin-logo"><h2>Admin Panel</h2></div>
      <nav class="admin-nav">
        <ul>
          <li class="nav-item"><a href="/admin" class="nav-link">📊 Dashboard</a></li>
          <li class="nav-item"><a href="/admin/appointments" class="nav-link">📅 Appointments</a></li>
          <li class="nav-item"><a href="/admin/customers" class="nav-link">👥 Customers</a></li>
          <li class="nav-item"><a href="/admin/staff" class="nav-link">👔 Staff</a></li>
          <li class="nav-item active"><a href="/admin/services" class="nav-link">💇 Services</a></li>
          <li class="nav-item"><a href="/admin/reports" class="nav-link">📈 Reports</a></li>
        </ul>
      </nav>
      <div class="admin-user-info">
        <div class="user-avatar"><%= user.givenName.charAt(0) %></div>
        <div class="user-details">
          <p class="user-name"><%= user.givenName %> <%= user.familyName %></p>
        </div>
      </div>
    </aside>

    <main class="admin-main">
      <div class="admin-topbar">
        <h1 class="page-title"><%= title %></h1>
        <div class="topbar-actions">
          <a href="/auth/logout" class="btn btn-logout">Logout</a>
        </div>
      </div>

      <div class="admin-content">
        <div class="section">
          <div class="section-header">
            <h2>Available Services</h2>
            <span><%= services.length %> services</span>
          </div>

          <% if (services && services.length > 0) { %>
            <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 15px; margin-top: 20px;">
              <% services.forEach((service) => { %>
                <div style="border: 1px solid #bdc3c7; border-radius: 8px; padding: 15px; background: white;">
                  <h3 style="margin-top: 0;"><%= service.name %></h3>
                  <div style="margin: 15px 0;">
                    <div style="display: flex; justify-content: space-between; margin-bottom: 8px;">
                      <span>Price:</span>
                      <strong><%= formatCurrency(service.price) %></strong>
                    </div>
                    <div style="display: flex; justify-content: space-between;">
                      <span>Duration:</span>
                      <strong><%= formatDuration(service.duration) %></strong>
                    </div>
                  </div>
                  <button class="btn btn-primary" style="width: 100%; margin-top: 10px;">Edit Service</button>
                </div>
              <% }); %>
            </div>
          <% } else { %>
            <div class="empty-state">
              <p>No services found.</p>
            </div>
          <% } %>
        </div>
      </div>
    </main>
  </div>
</body>
</html>
