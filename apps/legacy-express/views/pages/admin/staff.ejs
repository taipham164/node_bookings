<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="stylesheet" href="/stylesheets/admin-dashboard.css">
</head>
<body>
  <div class="admin-container">
    <aside class="admin-sidebar">
      <div class="admin-logo"><h2>Admin Panel</h2></div>
      <nav class="admin-nav">
        <ul>
          <li class="nav-item"><a href="/admin" class="nav-link">📊 Dashboard</a></li>
          <li class="nav-item"><a href="/admin/appointments" class="nav-link">📅 Appointments</a></li>
          <li class="nav-item"><a href="/admin/customers" class="nav-link">👥 Customers</a></li>
          <li class="nav-item active"><a href="/admin/staff" class="nav-link">👔 Staff</a></li>
          <li class="nav-item"><a href="/admin/services" class="nav-link">💇 Services</a></li>
          <li class="nav-item"><a href="/admin/reports" class="nav-link">📈 Reports</a></li>
        </ul>
      </nav>
      <div class="admin-user-info">
        <div class="user-avatar"><%= user.givenName.charAt(0) %></div>
        <div class="user-details">
          <p class="user-name"><%= user.givenName %> <%= user.familyName %></p>
        </div>
      </div>
    </aside>

    <main class="admin-main">
      <div class="admin-topbar">
        <h1 class="page-title"><%= title %></h1>
        <div class="topbar-actions">
          <a href="/auth/logout" class="btn btn-logout">Logout</a>
        </div>
      </div>

      <div class="admin-content">
        <div class="section">
          <div class="section-header">
            <h2>Staff Members</h2>
            <span><%= staff.length %> team members</span>
          </div>

          <% if (staff && staff.length > 0) { %>
            <table class="admin-table">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Display Name</th>
                  <th>Status</th>
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody>
                <% staff.forEach((member) => { %>
                  <tr>
                    <td><%= member.teamMemberId.substring(0, 8) %>...</td>
                    <td><%= member.displayName || 'N/A' %></td>
                    <td><span class="status-confirmed">Active</span></td>
                    <td>
                      <button class="btn-small btn-view">View Schedule</button>
                    </td>
                  </tr>
                <% }); %>
              </tbody>
            </table>
          <% } else { %>
            <div class="empty-state">
              <p>No staff members found.</p>
            </div>
          <% } %>
        </div>

        <!-- Staff Performance -->
        <div class="section">
          <h2>Performance Overview</h2>
          <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 15px;">
            <div style="padding: 15px; background: var(--light-bg); border-radius: 6px;">
              <h4>Total Services This Month</h4>
              <div style="font-size: 28px; font-weight: bold; color: var(--secondary-color); margin-top: 10px;">-</div>
              <p style="font-size: 12px; color: #7f8c8d; margin: 5px 0 0;">Click on a staff member for details</p>
            </div>
            <div style="padding: 15px; background: var(--light-bg); border-radius: 6px;">
              <h4>Customer Satisfaction</h4>
              <div style="font-size: 28px; font-weight: bold; color: var(--success-color); margin-top: 10px;">4.8/5.0</div>
              <p style="font-size: 12px; color: #7f8c8d; margin: 5px 0 0;">Based on customer reviews</p>
            </div>
          </div>
        </div>
      </div>
    </main>
  </div>
</body>
</html>
